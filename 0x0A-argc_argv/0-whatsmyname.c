#include<stdio.h>
/**
 * main - the entry to the program
 * @argc: the number of arguments
 * @argv: the arguments
 *
 * return: zero indicates success i guess
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
}
