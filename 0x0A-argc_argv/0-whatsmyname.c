#include <stdio.h>

/**
 * main - entry point
 * @argc: the counter
 * @argv: the argument array
 *
 * Return: always 0
 */
int main(int argc,char **argv)
{
	(void)argc:
	printf("%s\n" , argv[0]);
	return (0);

}
