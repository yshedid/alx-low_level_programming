#include <stdio.h>

/**
 * main - entry point
 * @argc: argument counter
 * @argv: the argument array
 *
 * Return: always 0
 */
int main(int argc,char **argv)
{
	pritnf("%s\n", argv[0]);
	return (0);

}
